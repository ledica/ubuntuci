version: 2
jobs:
  deploy-dev:
    docker:
      - image: borgesdemeirelles/ruby-node:0.1
        environment:
          PGHOST: 127.0.0.1
          PGUSER: postgres

    working_directory: ~/cicd-circleci

    steps:
      - checkout

workflows:
  version: 2
  deploy:
    jobs:
      - deploy-dev:
